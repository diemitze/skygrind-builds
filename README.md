# SkyGrind builds

Built jars only. The source lives elsewhere and is private.

`latest.json` in this folder is what an installed client polls to find out whether it is behind. Releases hold the jars themselves.
